# Copyright (c) Advanced Micro Devices, Inc. All rights reserved.
#
# SPDX-License-Identifier: MIT

name: deb-build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    paths:
      - '**.hip'
      - '**.cpp'
      - '**.c'
      - '**.h'
      - '**.hpp'
      - '**.cc'
      - 'CMakeLists.txt'
      - 'cmake/**'
      - 'debian/**'
      - 'kernel/**'
      - 'rdma-core/**'
      - 'scripts/**'
      - 'src/**'
      - 'udev/**'
      - '.github/actions/setup-rocm-build/**'
      - '.github/workflows/deb-build.yml'
  workflow_dispatch:
    inputs:
      offload_arch:
        description: >-
          GPU offload architectures (semicolon-separated).
          Leave empty to use the default from debian/rules.
        required: false

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-debs:
    name: Build .deb packages
    runs-on: ubuntu-latest
    container:
      image: rocm/dev-ubuntu-24.04:7.2
      options: --user root
    env:
      LD_LIBRARY_PATH: /opt/rocm/lib:/usr/lib/x86_64-linux-gnu
      GEN_CHANGELOG_ROLLING: '1'

    steps:
    - name: Check out code
      uses: actions/checkout@v5.0.0
      with:
        fetch-depth: 0

    - name: Set offload architectures
      if: inputs.offload_arch != ''
      run: |
        echo "DEB_OFFLOAD_ARCH=${{ inputs.offload_arch }}" \
          >> "$GITHUB_ENV"

    - name: Install build dependencies
      uses: ./.github/actions/setup-rocm-build
      with:
        apt-packages: >-
          devscripts
          equivs
          lsb-release
          python3-pip
          python3-venv

    - name: Install Debian package dependencies
      run: |
        mk-build-deps --install --remove \
          --tool 'apt-get -y --no-install-recommends' \
          debian/control

    - name: Install rocm-kpack Python tools
      run: |
        python3 -m venv .venv
        . .venv/bin/activate
        python -m pip install --upgrade pip
        python -m pip install \
          "rocm-kpack @ git+https://github.com/ROCm/rocm-kpack.git@main"
        echo "$PWD/.venv/bin" >> "$GITHUB_PATH"

    - name: Generate debian/changelog
      run: debian/gen-changelog.sh

    - name: Show generated changelog
      run: cat debian/changelog

    - name: Show generated release notes
      run: cat debian/generated-release-notes.md

    - name: Build .deb packages
      run: dpkg-buildpackage -us -uc -b

    - name: List built packages
      run: |
        echo "=== Built .deb files ==="
        ls -lh ../*.deb
        echo ""
        echo "=== Package contents ==="
        for deb in ../*.deb; do
          echo "--- $(basename "$deb") ---"
          dpkg-deb -c "$deb"
          echo ""
        done

    - name: Stage built packages
      run: |
        mkdir -p built-debs
        cp debian/generated-release-notes.md built-debs/generated-release-notes.md
        for f in ../*.deb ../*.buildinfo ../*.changes; do
          if [ -f "$f" ]; then
            base="$(basename "$f")"
            cp "$f" "built-debs/rocm-xio-latest-${base}"
          fi
        done

    - name: Upload .deb artifacts
      uses: actions/upload-artifact@v4
      with:
        name: rocm-xio-debs-rolling-${{ github.sha }}
        path: built-debs/

  publish-rolling:
    name: Update rolling package pre-release
    needs: build-debs
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Download .deb artifacts
      uses: actions/download-artifact@v4
      with:
        name: rocm-xio-debs-rolling-${{ github.sha }}
        path: built-debs/

    - name: Update rolling package pre-release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: rocm-xio-latest
        name: Pre-release packages - tag rocm-xio-latest
        body_path: built-debs/generated-release-notes.md
        prerelease: true
        make_latest: false
        files: built-debs/rocm-xio-latest-*
